// prisma/schema.prisma

datasource db {
  provider = "postgresql" // Adjust to your DB type
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model UserModel {
  id       Int    @id @default(autoincrement())
  email    String @unique
  password String
}
